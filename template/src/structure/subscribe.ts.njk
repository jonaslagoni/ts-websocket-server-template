import {Channel} from '../WebsocketConnections'
export default class Subscribe {
    public channel?:Channel;
    constructor();
    constructor(
        channel?:Channel
        ) {
        this.channel= channel;
    }

    copyInto(jsonString: any){
        this.channel=jsonString.channel;
    }
}